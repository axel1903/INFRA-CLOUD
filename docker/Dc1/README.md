# Dc1 – Run containers experiment

## Doel
Het uitvoeren van Docker containers met behulp van het `docker run` commando.

## Beschrijving
In dit experiment werd het starten van Docker containers getest met:
- een zelf aangemaakt Docker image (di2-hello)
- een standaard Docker image (hello-world)

## Uitvoering
```bash
docker run di2-hello
docker run hello-world
