# AP5 – REST API experiment with curl

## Doel
Het uitvoeren van REST API requests met behulp van curl.

## Beschrijving
In dit experiment werd een publieke REST API gebruikt
(jsonplaceholder.typicode.com) om REST requests te testen.

## Uitvoering
GET request:
```bash
curl -X GET https://jsonplaceholder.typicode.com/posts/1
